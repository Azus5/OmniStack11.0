import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//O Que faz a pagina dar um refresh sempre que alteramos um arquivo, é chamado de live reload

ReactDOM.render(<App />, document.getElementById('root'));
